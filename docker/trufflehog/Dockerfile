# Not built -- this is a version pin, read by secret-scan.yml at runtime, so
# Dependabot has one real manifest to bump instead of a bash variable it
# can't see. Ported from mark-brannan/symphony's docker/trufflehog/Dockerfile
# (2026-09-05), the pattern this repo's secret-scan.yml was missing.
FROM trufflesecurity/trufflehog:3.97.0
